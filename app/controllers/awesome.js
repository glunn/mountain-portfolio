import Controller from '@ember/controller';

export default Controller.extend({
  typedStrings: ["Ember apps.", "RESTful APIs.", "React components.", "SQL queries.", "automated tests with Selenium.", "landing pages."]
});
